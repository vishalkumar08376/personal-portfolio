-- Creating Database
CREATE DATABASE vishal_portfolio;
USE vishal_portfolio;

-- Creating Table in Database 
CREATE TABLE contact (userName VARCHAR(50), emailId VARCHAR(50) PRIMARY KEY, Message VARCHAR(500));